{{/*
postgres-service.yaml - PostgreSQL Service
=============================================================================
*/}}
{{- if and .Values.postgres.enabled (not .Values.postgres.external.enabled) }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "neodem.postgres.fullname" . }}
  labels:
    {{- include "neodem.labels" . | nindent 4 }}
    app.kubernetes.io/component: postgres
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: postgres
      protocol: TCP
      name: postgres
  selector:
    {{- include "neodem.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: postgres
{{- end }}
