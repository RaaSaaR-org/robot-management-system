"""Unitree-DDS simulation node for the G1 EDU + Dex3-1.

Speaks the real robot's wire protocol against a MuJoCo scene, so unmodified
`unitree_sdk2py` scripts -- and the robot-agent's own hardware seam -- can be
exercised with no hardware attached. See README.md for the build recipe.
"""
