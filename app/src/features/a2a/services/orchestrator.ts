/**
 * @file orchestrator.ts
 * @description Orchestrator service — LLM agent selection is now handled server-side.
 * This file is kept as a stub for backwards compatibility.
 * @feature a2a
 */

// Agent selection via LLM is now handled by the server's ConversationManager.
// The frontend always uses the /a2a/message/orchestrate endpoint in orchestration mode.
